L2를 로직에 넣고 AdamW이면 여기에 L2를, Adam이면 따로 L2로직에 이를 포함한다
3번이상 loss가 줄지 않으면 lr:근데 3번해서 또 다시 줄면 좀 더 강하게 

lr과 Batch는 비례 관계이다

 Train acc의 암기가 100%까지 매우 빠르면 규제 강화 (Weight Decay & Dropout)을 더 높게 줘야한다

전체 데이터를 배치 사이즈로 나눈 몫이 1에폭당 필요한 이터레이션(Iteration) 횟수가 되는데 이게 완전히 나눠 떨어져야한다 즉, train data의 갯수가 배치 사이즈로 나눠 떨어져야한다
iter_per_epoch = max(train_size // batch_size==0이어야한다, 1)
이에 따라 batchNorm도 문제가 생긴다 배치 사이즈가 256일 때 최적화되도록 가중치 감쇠(Weight Decay)나 배치 정규화(BatchNorm)를 세팅해 놨는데, 갑자기 마지막에 232개짜리 족보 없는 사이즈가 들어오면 그라디언트의 크기가 요동치거나 통계치가 왜곡되어 학습이 불안정해질 수 있기 때문 
#TODO: get_loss_in_batches을 .loss를 로직적으로 더 좋게 바꿀 순 없는가?